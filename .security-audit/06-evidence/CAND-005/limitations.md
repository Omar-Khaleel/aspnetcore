# Limitations

Dynamic test execution and fuzzing were not possible because the repository SDK bootstrap could not download through the configured proxy. This candidate is rejected for the concrete binder-bypass/unsafe-default hypothesis based on source and existing test evidence, but broader SignalR fuzzing remains a coverage gap.
