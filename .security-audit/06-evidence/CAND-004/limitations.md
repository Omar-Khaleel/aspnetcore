# Limitations

- No Kestrel unit tests or fuzzing targets were executed because `./restore.sh` could not install the repository SDK through the configured proxy.
- No front-end/proxy differential harness was run.
- This rejection applies to the concrete TE/CL and final-transfer-coding hypothesis reviewed here, not to all possible Kestrel parser/state-machine bugs.
